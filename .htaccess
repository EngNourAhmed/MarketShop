<IfModule mod_rewrite.c>
    RewriteEngine On

    # Redirect root to shop home
    RewriteRule ^$ public/shop [R=302,L]

    # Route everything else through Laravel public
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
